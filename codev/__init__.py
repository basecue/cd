
from jinja2 import Template



def install_project(configuration, environment, infrastructure):
    configuration.provider

    return


    # spamc = subprocess.call(ansible-playbook, stdin=subprocess.PIPE, stdout=subprocess.PIPE)
    # spamc.stdin.write(raw_message)
    # spamc.stdin.close()
    # spamc.wait()
    # test_time = timedelta_total_seconds(datetime.now() - start)
    #
    # result = spamc.stdout.read()
    #  -i conf/provisioning/hosts conf/provisioning/server.yml

    #get source from repository
    # if environment == 'development':
    #
    # working_dir = path.join(PROJECTS_DIRECTORY, project_name)
    # repo.clone()
    # PROJECTS_DIRECTORY
    #
    # platform.machine()
    # 'x86_64'
    # platform.dist()
    # ('Ubuntu', '15.10', 'wily')
    #
    #    # import lxc
    #
    # # Setup the container object
    # container = lxc.Container("name")
    # if container.defined:
    #     pass
    #
    #
    # if not container.create("download", lxc.LXC_CREATE_QUIET, {"dist": "ubuntu",
    #                                                "release": "trusty",
    #                                                "arch": "i386"}):
    # print("Failed to create the container rootfs", file=sys.stderr)
    # sys.exit(1)
    # pass
    # from .usecases import InstallProject
    # install_project = InstallProject(project_name, version=version, environment=environment)